apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: task-manager
spec:
  selector:
    app: auth-service
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
